export const metadata = {
  title: "Connexion — Sigma Factory",
  robots: { index: false, follow: false },
};

export default function SigmaLoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
