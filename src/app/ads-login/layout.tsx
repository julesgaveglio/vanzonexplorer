export const metadata = {
  title: "Connexion — Vanzon Ads",
  robots: { index: false, follow: false },
};

export default function AdsLoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
