// Re-export sigma auth helpers for use in layout (server component)
// The canonical auth logic lives in src/app/api/sigma/_helpers/auth.ts
export { getSigmaSession } from "@/app/api/sigma/_helpers/auth";
